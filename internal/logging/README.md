# logging

This package provides a global, configurable logger using zerolog. It supports JSON and colored console formats, multiple outputs (stdout, file, etc.), and easy integration with trace IDs and span IDs for distributed tracing.